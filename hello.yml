name: Test ARC Runner

on:
  push:
    branches: [ main ]

jobs:
  say-hello:
    runs-on: self-hosted  # This tells GitHub to use your ARC runner
    steps:
      - name: Print Hello
        run: echo "🎉 Hello from your ARC self-hosted runner!"
